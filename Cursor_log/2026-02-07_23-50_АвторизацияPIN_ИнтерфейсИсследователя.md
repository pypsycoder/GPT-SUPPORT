**Задача:** Модуль авторизации пациентов (PIN+ID) и интерфейс исследователя

**Дата:** 2026-02-07 23:50

**Действия:**
- Добавлены поля PIN-авторизации в модель User (patient_number, pin_hash, pin_attempts, is_locked, consent_given_at)
- Создана модель Researcher (username, password_hash, full_name, is_active)
- Создан модуль auth (security, service, router, dependencies) — авторизация через HttpOnly cookies
- Создан модуль consent (router, service, schemas) — управление согласиями
- Создан модуль researchers (models, crud, router, schemas) — CRUD пациентов, генерация PIN
- Создан frontend: login.html + consent.html для пациентов (крупный шрифт, простой UX)
- Создан frontend исследователя: login, dashboard со статистикой, список пациентов, создание/блокировка/сброс PIN
- Обновлены все существующие страницы пациента для работы с session-based авторизацией
- Обновлен pages router: новые маршруты /patient/*, /researcher/*, /login, /consent + legacy /p/{token}/*
- Создана Alembic миграция для новых полей и таблиц
- Создан скрипт create_researcher.py для создания первого аккаунта исследователя
- Добавлена зависимость passlib[bcrypt] в requirements.txt

**Файлы:**
- `app/users/models.py` (изменён — добавлены поля PIN-авторизации)
- `app/auth/__init__.py`, `security.py`, `service.py`, `router.py`, `schemas.py`, `dependencies.py` (созданы)
- `app/consent/__init__.py`, `router.py`, `service.py`, `schemas.py` (созданы)
- `app/researchers/__init__.py`, `models.py`, `crud.py`, `router.py`, `schemas.py` (созданы)
- `app/pages/router.py` (переписан — новые маршруты + legacy)
- `app/main.py` (изменён — регистрация новых роутеров)
- `app/models/__init__.py` (изменён — регистрация модели Researcher)
- `alembic/env.py` (изменён — импорт researchers.models)
- `alembic/versions/f1a2b3c4d5e6_add_pin_auth_and_researcher.py` (создан)
- `frontend/patient/login.html`, `consent.html` (созданы)
- `frontend/patient/css/login.css`, `consent.css` (созданы)
- `frontend/patient/js/login.js`, `consent.js`, `auth.js` (созданы)
- `frontend/researcher/login.html`, `dashboard.html` (созданы)
- `frontend/researcher/css/researcher.css` (создан)
- `frontend/researcher/js/auth.js`, `patients.js` (созданы)
- `frontend/patient/js/*.js` (обновлены — session-based auth)
- `frontend/patient/*.html` (обновлены — подключен auth.js)
- `scripts/create_researcher.py` (создан)
- `requirements.txt` (изменён — passlib[bcrypt])

**Результат:** Полностью реализован модуль авторизации PIN+ID и интерфейс исследователя. Для запуска нужно: 1) pip install passlib[bcrypt], 2) alembic upgrade head, 3) python -m scripts.create_researcher --username admin --password <пароль>

**Проблемы:** Нет
