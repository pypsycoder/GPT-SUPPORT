**Задача:** Применение миграции БД для таблицы scale_results (PSQI)

**Дата:** 2026-02-07 21:30

**Действия:**
- Диагностированы 3 проблемы: ветвление миграций (2 head), отсутствие uuid-ossp, таблица уже существует в БД
- Исправлен `down_revision` с `86a0d2c1f8e3` на `d2ca01903a4c` (убрано ветвление)
- Заменён `uuid_generate_v4()` на `gen_random_uuid()` (встроенная функция PostgreSQL)
- Таблица уже была в БД — выполнен stamp + ручное обновление alembic_version

**Файлы:**
- `alembic/versions/3d3b0b2c7bf8_add_scale_results_table.py` (изменён)

**Результат:** Alembic current = `3d3b0b2c7bf8 (head)`. Таблица `scales.scale_results` синхронизирована с миграцией. UUID default = `gen_random_uuid()`.

**Проблемы:** Нет
