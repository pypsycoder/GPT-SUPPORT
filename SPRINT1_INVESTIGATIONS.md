# Спринт 1 — отчёты по investigation-задачам #1 и #2

Дата: 2026-08-29. Ветка `feat/agents-rework`.
Метод: анализ `llm.llm_call_log` / `llm.llm_request_logs` за всё время + свежий
принудительный вызов по тирам. Скрипты — разовые, в репозиторий не входят.

---

## #1 — Аудит аккаунта GigaChat

**Вопрос:** обслуживает ли персональный ключ модели Pro/Max? Каков реальный лимит
одновременных потоков? Нужен ли 2-й аккаунт.

**Вводная от заказчика (2026-08-29):** ключ один, второй аккаунт / ИП пока не
планируется. → выводы применяем к одноключевой конфигурации.

**Пересмотрено (2026-09-02):** (1) добавлен 2-й ключ `GIGACHAT_KEY_L1` (аккаунт
Лены); (2) появился рабочий `CLOUD_RU_KEY` — решение перевести контур на Cloud.ru
Foundation Models, Сбер PERS/Pro/Max — прозапас. См. оба «Дополнения 2026-09-02»
в конце §1.

### Обслуживает ли ключ Pro / Max — ДА

Свежая проверка (2026-08-29, принудительный тир, `session_id=None`):

| Тир | Модель в запросе | Результат | Задержка |
|---|---|---|---|
| lite | `GigaChat-2` | OK | ~16 с (первый вызов — прогрев OAuth) |
| pro | `GigaChat-2-Pro` | OK | 171 мс |
| max | `GigaChat-2-Max` | OK | 141 мс |

Историческое подтверждение (`llm.llm_call_log`, `ok = true`):

| Модель | Успешных вызовов | Период |
|---|---|---|
| `GigaChat-2` | 2251 | 10–29 авг |
| `GigaChat-2-Pro` | 4309 | 10–27 авг |
| `GigaChat-2-Max` | 2955 | 10–27 авг |

Ни одного отказа авторизации (401/403) за всё время. Персональный scope
`GIGACHAT_API_PERS` даёт все три модели.

### Лимит одновременных потоков — 1 (+ низкий RPM)

- В коде конкурентность уже равна 1: `AccountPool` при единственном ключе плодит
  алиасы `A1 / A1-pro / A1-max` на **общем** `_SharedAccountState` → один
  `asyncio.Lock` на все тиры (подтверждено: `len({id(c._state)}) == 1`).
- Все сбои по API — `429 Too Many Requests`, никогда не `4xx` авторизации:
  `GigaChat-2-Max` — 35, `GigaChat-2` — 15, `GigaChat-2-Pro` — 11 (плюс 11×`500`).
- 429-е приходят **только пачками** во время batch-прогонов
  (11 авг 00:24–00:26 — 34×Max за 3 минуты; 26 авг 23:30–23:33 — смешанный
  всплеск). Органический однопользовательский чат в лимит не упирается: пиковая
  секунда за всю историю — 5 успешных вызовов, и то роутерные (lite).

### Вывод

- **2-й аккаунт для реактивного контура не нужен** — lock=1 + низкий RPM хватает
  на один живой диалог, кэш работает (см. #2).
- **Для проактивной рассылки — узкое место.** `scheduler._CONCURRENCY = 5`:
  семафор пускает 5 пациентов параллельно, каждый пайплайн — несколько
  последовательных вызовов, всё через один lock; на ретраях/репейрах пачка легко
  превышает RPM → 429. Раз второго ключа не будет, смягчаем кодом:
  1. `scheduler._CONCURRENCY = 1` для проактивных джоб (или отдельный семафор).
  2. Джиттер между пациентами (0.5–1 с).
  3. Backoff на 429 в `pool._execute` (сейчас ретрай только по токену/транспорту,
     429 сразу пробрасывается как `LLMResponseError`).
  4. Разнести утро/день/вечер по времени и не совмещать с cron-страховкой.
  → отдельная задача, в блокеры спринта не выносится.

### Дополнение 2026-09-02 — второй ключ

Заказчик добавил `GIGACHAT_KEY_L1` (2-й персональный аккаунт). Решение
«2-го ключа не будет» отменено.

**Что сделано в коде:**

- `AccountPool` обобщён: аккаунт заводится из любого `GIGACHAT_KEY_<id>`
  (не только `A1..A19`), дедуп по значению ключа, детерминированный порядок
  по `account_id`. Каждый аккаунт получает свой `_SharedAccountState`
  (→ свой `asyncio.Lock`) и поднимается под все три тира —
  алиасы `<id>-lite/-pro/-max` делят один лок аккаунта.
  `GIGACHAT_MODEL_<id>` остаётся ограничителем «только один тир».
- `pool.account_count` — число физических локов = потолок конкурентности.
- `scheduler._PROACTIVE_CONCURRENCY = pool.account_count` (было жёстко 1).

**Проверка (2026-09-02, принудительный тир по каждому клиенту):**

| Аккаунт | lite | pro | max |
|---|---|---|---|
| `A1` (Дима) | OK | OK | OK |
| `L1` (Лена) | OK | OK | OK |

Оба ключа авторизуются и обслуживают все три модели. Конкурентность пула
= 2.

**Что это даёт:** потолок одновременных ходов 1 → 2 (линейно по числу
ключей). Латентность одного хода не меняется (цепочка вызовов внутри хода
последовательна). Для «десятков пациентов разом» по-прежнему нужен аккаунт
юрлица (10 потоков/ключ); добавление ключей #3+ пул подхватит автоматически.

**Телеметрия:** `llm_call_log.account_id` для lite-клиента теперь `A1-lite`
(был `A1` без суффикса) — единоразовый шов в аналитике по `account_id`.

### Дополнение 2026-09-02 (2) — Cloud.ru как провайдер

Появился ключ `CLOUD_RU_KEY` — Cloud.ru Evolution Foundation Models
(OpenAI-совместимый шлюз `https://foundation-models.api.cloud.ru/v1`, Bearer =
ключ `<keyid>.<secret>` целиком, без обмена на токен).

**Активация ключа:** мало роли сервисного аккаунта — при создании API-ключа
надо явно привязать **сервис «Foundation Models»** (иначе `/v1/models` отдаёт
каталог, но `/v1/chat/completions` → `403 AccessDenied: Key does not have access
to this product`). `/v1/models` не аутентифицирован — для проверки доступа
бесполезен.

**Ёмкость (burst-проба, GigaChat 3.5 Ultra, один ключ):**

| Параллельно | Результат |
|---|---|
| 8  | 8/8 OK |
| 16 | 16/16 OK |
| 24 | 22/24 OK, 2× `429` |

Лимит — RPM/TPM на **аккаунт/проект**, не «1 поток/ключ» как у Сбера PERS.
~16 параллельных из коробки, повышается через поддержку. Несколько ключей под
одним аккаунтом квоту **не умножают** — «наплодить ключей» смысла нет.

**Каталог:** `ai-sage/GigaChat3.5-432B-A28B` (= GigaChat 3.5 Ultra, флагман),
`GigaChat/GigaChat-2-Max`, `ai-sage/GigaChat3-10B-A1.8B` (Lite), плюс Qwen3 /
DeepSeek / GLM / gpt-oss. ⚠ `GigaChat-2-Max` встроенно цензурирует суицид-контент
(«Извините, не могу ответить») — в safety-слой не годится.

**Safety-бенч** (`d:/PROJECT/safety-bench`, golden holdout, продовая рубрика,
runs=3, t=0): GigaChat 3.5 Ultra recall {act,plan} **0.97** / FPR **0.00** против
GigaChat-2-Pro 0.86 / 0.00 и текущего прода (2-Lite) 0.78 / 0.02. Разрыв — модель.

### Сбер тоже сдвинулся: новый endpoint + GigaChat 3 Ultra в Freemium

С **17.07.2026** базовый адрес GigaChat API — `https://api.giga.chat`
(старый `gigachat.devices.sberbank.ru` ещё отвечает, но новые модели не отдаёт:
на нём только `GigaChat` / `GigaChat-2` / `-2-Pro` / `-2-Max` + preview).
OAuth-токен тот же (`ngw.devices.sberbank.ru`, scope PERS).

Физлицам в **Freemium** доступен **GigaChat 3 Ultra** (702B, 128k контекст,
~1 млн бесплатных токенов) — это те «токены в ЛК», о которых спросил Дмитрий.

⚠ `api.giga.chat` **не открывается с dev-машины** — TLS handshake timeout
(DNS резолвится в `155.212.234.167`, старый endpoint при этом работает). Проверку
Сбер-Ultra и перевод `sbench/gigachat.py` на новый адрес — со staging/прода.

**Итоговое решение:** оба провайдера в коде, переключение флагом `LLM_PROVIDER`
+ кнопка в researcher-панели. Прод по умолчанию — Cloud.ru (GigaChat 3.5 Ultra,
конкурентность), Сбер PERS — резерв и источник для тестов (Freemium). План —
`ROADMAP_AGENT.md` → Фаза 6.

---

## #2 — Замер префиксного кэша

**Вопрос:** какова доля `precached_tokens` на 2-м+ ходу треда? Стабильный ноль
означал бы нестабильный байт в префиксе.

**Формула:** `hit = precached / (precached + prompt)`. В `usage` GigaChat
`prompt_tokens` и `precached_prompt_tokens` **дизъюнктны** (в агрегате precached
регулярно превышает prompt — значит это не подмножество).

**Ранг вызова:** `row_number() OVER (PARTITION BY session_key, prefix_fp ORDER BY
created_at)`. rn=1 — холодный, rn≥2 — тёплый.

### Результат (14 дней, только `ok`, есть `session_key`)

| Ход | Вызовов | hit (по токенам) | hit (среднее по вызову) |
|---|---|---|---|
| 1 — холодный | 254 | 39.5 % | 40.4 % |
| 2+ — тёплый | 7536 | **80.3 %** | 78.7 % |

Кэш работает. «Стабильного нуля» нет.

### По шагам (тёплые вызовы)

| Шаг | Модель | Вызовов | hit |
|---|---|---|---|
| `agent` | Pro | 1672 | 88.5 % |
| `agent_tools` | Pro | 1507 | 77.5 % |
| `agent_repair` | Pro | 152 | 96.3 % |
| `router_l2` | lite | 1944 | 76.0 % |
| `judge` | Max | 1914 | 73.6 % |
| `agent_tools` | Max | 94 | 31.6 % ⚠ → разобрано, см. «Что чинить» п. 2 |
| `supervisor` | Pro | 16 | 18.3 % ⚠ (старая, малая выборка) |

### Стабильность префикса — OK

Запрос «сколько разных `prefix_fp` на один `session_key`+`step`» вернул **пусто**:
ни в одном треде отпечаток стабильной части не «дышит». Нестабильных фрагментов
(uuid / время / iso-дата / epoch / хвостовой ноль) в слоях 0–2 нет.

### Что чинить (низкий приоритет)

1. ~~**`summarizer` / `summarizer_repair` не передают `X-Session-ID`**~~
   Исправлено 2026-08-30: `memory_store.maybe_compact` зовёт `client.structured`
   с `session_id="summarizer-shared"` (системный промпт суммаризатора
   константный — общий кэш, как у judge). Repair-вызов наследует тот же
   `session_id` внутри `structured()`.
2. **`agent_tools` на Max — холодный по своей природе. Проверено, пункт
   закрыт.** Свежий прогон `patient-sim` (2026-09-02, граница
   `llm_call_log.id > 10432`): Max этим шагом ещё пользуется, но 1 тёплый
   вызов за весь прогон, hit 0.1 %. Августовские «94 вызова / 31.6 %» — это
   тоже трафик `patient-sim` (`session_key` вида `p6-sim-…`), не органика;
   31.6 % = смесь холодных первых Pro→Max переключений (~0 %) и последующих
   Max-вызовов того же треда (~90 %).

   Причина — **не** нестабильный префикс (`prefix_fp` в треде стабилен,
   `distinct_fp = 1`), а смена тира в середине треда. Тред идёт на Pro, и
   только кризисный ход бампает `tier → max` (SAFETY-классификация). Серверный
   префикс-кэш GigaChat привязан к алиасу-модели: `A1-pro` и `A1-max` —
   раздельные кэши, первый Max-вызов треда всегда холодный. А `agent_tools`
   в ходе идёт **до** структурного финала `agent`
   (`loop.py` `_collect_with_tools` → `structured`), поэтому именно он греет
   Max за свой счёт: в том же ходу следующий `agent` на Max уже попадает в
   кэш (id 10441: precached 1300, hit 83.8 %).

   Объём мизерный (1 вызов на прогон, ~96 за всё время наблюдения). Фикс, если
   когда-нибудь понадобится: держать фазу сбора инструментами на Pro даже для
   кризисного хода, Max отдавать только структурный финал. Приоритет низкий,
   фикс не планируется.
