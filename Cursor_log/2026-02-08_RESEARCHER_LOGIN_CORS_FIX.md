**Задача:** Исправить перезагрузку страницы логина исследователя вместо редиректа на dashboard

**Дата:** 2026-02-08 (режим DEBUG)

**Корень проблемы:** Отсутствовал CORS middleware в FastAPI приложении, из-за чего браузер блокировал fetch запросы к API endpoint. Был 401 Unauthorized вместо ожидаемого 200 OK.

**Действия:**
- Добавлен CORSMiddleware в `app/main.py` с поддержкой localhost и 127.0.0.1
- Разрешены credentials для правильной работы cookies
- Перезапущен бэкенд с новой конфигурацией
- Удалена инструментация отладки из `auth.js` после подтверждения работы

**Файлы:**
- `app/main.py` (изменён - добавлен CORS)
- `frontend/researcher/js/auth.js` (изменён - удалена инструментация после проверки)

**Результат:** ✅ Логин исследователя работает корректно. Успешная аутентификация редиректит на dashboard без перезагрузки логина.

**Логи подтверждения:** Строки 73-79 debug.log показывают полный цикл:
- Form submit + preventDefault ✅
- API response 200 OK ✅
- Redirect triggered → `/researcher/dashboard` ✅

**Проблемы:** Нет
