# ─────────────────────────────────────────────────────────────────────────────
#  GPT Health Support — пример окружения.
#  Скопируйте в .env и подставьте реальные значения. .env в git не коммитим.
# ─────────────────────────────────────────────────────────────────────────────

# --- Базовое -----------------------------------------------------------------
ENVIRONMENT=dev                     # dev | test | prod
# APP_NAME=GPT Health Support
# ENV_FILE=.env                     # переопределить путь к файлу окружения

DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/hemo_db

# --- Проактивный планировщик ----------------------------------------------- -
# OFF по умолчанию. true → в lifespan app/main.py стартует APScheduler:
# проактивный координатор в 08:00 / 14:00 / 20:00 МСК (собирает поводы от всех
# подсистем, единый потолок 2/день, дедуп в llm.proactive_deliveries).
# Тот же флаг требует и запасной процесс `python -m app.llm.worker`.
# Двойного запуска не будет: оба пути берут один advisory-lock.
SCHEDULER_ENABLED=false
# Идентификатор advisory-lock (pg_try_advisory_lock). Менять только если он
# конфликтует с другим приложением на той же БД.
# SCHEDULER_LOCK_ID=4815162342

# --- LLM-классификатор суицид-риска (2-й эшелон после L0-regex) ------------ -
# ON по умолчанию (kill-switch: LLM_SAFETY_LLM=false). GigaChat-2 Lite, рубрика
# app/llm/prompts/safety_classifier.txt. См. SAFETY_LLM_INTEGRATION_PLAN.md.
# LLM_SAFETY_LLM=true

# --- Выбор LLM-провайдера чата ------------------------------------------- -
# sber (default) | cloudru — кем обслуживать чат-вызовы агента и классификаторов.
# ЭМБЕДДИНГИ (RAG, router L1) ВСЕГДА идут через Сбер (индекс построен на модели
# Embeddings) → при LLM_PROVIDER=cloudru всё равно нужен GIGACHAT_KEY_*.
# LLM_PROVIDER=sber

# --- GigaChat (аккаунты Сбербанка, base64-encoded credential) ------------- -
# Любой GIGACHAT_KEY_<id> = отдельный аккаунт со своим локом. N ключей =
# N одновременных потоков (PERS = 1 поток/ключ). Каждый аккаунт обслуживает
# все три тира (lite/pro/max) — суффикс тира в id не нужен.
GIGACHAT_KEY_A1=PUT-BASE64-CREDENTIAL-HERE
# GIGACHAT_KEY_L1=<base64_credential_account_2>   # 2-й аккаунт → конкурентность 2
# GIGACHAT_MODEL_A1=max           # необязательно: пришпилить аккаунт к одному тиру
# GIGACHAT_CERT_PATH=/path/to/russian_trusted_root_ca.pem   # иначе verify=False
# GIGACHAT_CHAT_URL=https://api.giga.chat/v1/chat/completions   # новый адрес Сбера
#   (с 17.07.2026, туда же GigaChat 3 Ultra Freemium). Default — старый endpoint.
#   ⚠ api.giga.chat не открывается с dev-машины — проверять со staging.

# --- Cloud.ru Evolution Foundation Models (LLM_PROVIDER=cloudru) ---------- -
# OpenAI-совместимый шлюз. Ключ формата <keyid>.<secret> идёт в Bearer как есть
# (без OAuth). Активируется ТОЛЬКО с привязкой сервиса «Foundation Models»
# к API-ключу (одной роли сервисного аккаунта мало).
# CLOUD_RU_KEY=<keyid>.<secret>
# CLOUD_RU_MODEL=ai-sage/GigaChat3.5-432B-A28B      # модель на все тиры (default)
# CLOUD_RU_MODEL_LITE=ai-sage/GigaChat3-10B-A1.8B   # переопределить отдельный тир
# CLOUD_RU_CONCURRENCY=8                            # одновременных запросов на ключ
# NB: safety-классификатор на cloudru берёт тир max (→ CLOUD_RU_MODEL_MAX,
#     по умолчанию GigaChat 3.5 Ultra) — не роняйте его удешевлением.

# --- Прочие LLM-провайдеры (patient-sim, вспомогательное) ----------------- -
# OPENROUTER_API_KEY=sk-or-v1-...
# ANTHROPIC_API_KEY=sk-ant-...

# --- Рейт-лимит чата ------------------------------------------------------- -
# In-process скользящее окно на пациента для POST /api/chat/message.
# CHAT_RATE_LIMIT_MAX=20            # запросов в окне; 0 = выключить
# CHAT_RATE_LIMIT_WINDOW_SEC=60

# --- Флаги разработки ----------------------------------------------------- -
# LLM_DEBUG=false                   # true → лог промптов/ответов эксперта
# CSRF_ENABLED=false
