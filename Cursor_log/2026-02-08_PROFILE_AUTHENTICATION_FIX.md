**Задача:** Исправить авторизацию и загрузку профиля пациента

**Дата:** 2026-02-08 15:40

**Действия:**
- Переделана авторизация профиля: с URL токена на session cookies
- Исправлена логика сервиса профиля: передача User объекта вместо токена
- Найдена и исправлена ошибка в Pydantic схеме: telegram_id может быть NULL
- Добавлено детальное логирование для отладки

**Файлы:**
- `frontend/patient/js/profile.js` (переделана авторизация)
- `frontend/patient/profile.html` (удален token из заголовка)
- `app/profile/router.py` (передача user объекта, логирование)
- `app/profile/service.py` (функции принимают User объект)
- `app/profile/schemas.py` (telegram_id теперь Optional)

**Результат:** Профиль теперь загружается корректно с session-based авторизацией

**Проблемы:** Git lock файл блокирует commit - нужно рестартовать процесс git
