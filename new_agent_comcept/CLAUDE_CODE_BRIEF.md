# Брифы для Claude Code

Готовые задания. Давать **по одному**, в порядке шагов. Каждое заканчивается
измеримым результатом. Не склеивай их в одну задачу — получишь кашу.

Перед первым запуском положи `ref/` куда-нибудь рядом с проектом (не внутрь
`app/`) и укажи путь в задании.

---

## Шаг 0. Телеметрия

```
Задача: добавить учёт расхода токенов, ничего больше не меняя.

Контекст: проект GPT-SUPPORT, FastAPI + SQLAlchemy 2.0 async + asyncpg,
паттерны в CLAUDE.md, эталон — app/vitals/.

Сделать:
1. Alembic-миграцию для таблицы agent.call_log по DDL из
   <путь>/ref/memory_schema.sql (раздел «ТЕЛЕМЕТРИЯ»).
   Миграцию НЕ применять — только сгенерировать, я применю сам.
2. В app/llm/pool.py в GigaChatClient.call() вернуть из ответа поле
   usage.precached_prompt_tokens (сейчас оно теряется).
3. Записывать каждый вызов в agent.call_log. Запись асинхронная и
   не должна ронять основной поток при ошибке БД.

Ограничения:
- Не менять сигнатуры существующих публичных функций.
- Не трогать app/gpt_support/.
- commit() только в router-слое, как в CLAUDE.md.

Результат: SQL-запрос, который показывает токены и cache_hit по часам.
```

---

## Шаг 1. Включить префиксный кэш

```
Задача: включить кэширование контекста GigaChat через X-Session-ID.

Матчасть: https://developers.sber.ru/docs/ru/gigachat/guides/keeping-context
Кэшированные токены вычитаются из тарифицируемых (поле total_tokens).
Сейчас заголовок не передаётся, поэтому кэш не работает никогда.

Сделать:
1. В app/llm/pool.py: параметр session_id в call(), при наличии —
   заголовок "X-Session-ID".
2. Все вызывающие места передают ключ f"p{patient_id}-{thread_id}".
   Найди их все: app/llm/pipeline/, app/llm/langgraph_supervisor/nodes.py,
   app/llm/proactive.py, app/routers/chat.py.
3. Sticky-роутинг аккаунтов: тред всегда идёт на один аккаунт
   (hash(thread_id) % len(accounts)), переключение только при отказе.
   Кэш живёт в контуре аккаунта — прыжки между аккаунтами его обнуляют.

Ограничения:
- В session_id не должно попадать ничего идентифицирующего пациента,
  кроме внутреннего числового id.
- Поведение при cache_hit=0 не должно ломаться.

Результат: покажи cache_hit из agent.call_log до и после на одном
и том же сценарии из LLM_test/.
```

---

## Шаг 2. Сборка промпта по слоям

```
Задача: пересобрать формирование messages так, чтобы префикс был стабилен.

Референс: <путь>/ref/prompt_assembly.py — читай его целиком, там правило
и список того, что кэш убивает.

Правило: messages собираются по убыванию стабильности, ничто выше
волатильного слоя не меняется в течение сессии.
  [0] system, [1] профиль, [2] свёртка, [3] окно, [4] RAG + реплика

Сделать:
1. Перенести сборку из app/llm/context_builder.py на PromptLayers.
2. Найти и вынести из стабильной части всё нестабильное:
   timestamp, uuid, json.dumps без sort_keys, RAG-контекст,
   форматирование float. Отчитайся списком найденного.
3. Логировать prefix_fingerprint() в agent.call_log.
4. GigaChat: ровно один system и только первым (иначе 422).
   Профиль и свёртка идут парой user/assistant после system.

Ограничения:
- context_builder.py не удалять, добавить новый путь рядом
  и переключать флагом окружения.

Результат: тест, доказывающий, что рост окна не меняет prefix_fingerprint.
```

---

## Шаг 3. Структурный вывод

```
Задача: заменить самописный парсинг ответов на response_format json_schema.

Матчасть: https://developers.sber.ru/docs/ru/gigachat/guides/structured-output
Референс: <путь>/ref/gigachat_client.py — методы structured() и json_schema_for().

Сейчас в app/llm/langgraph_supervisor/policy.py: _parse_field_block(),
_build_*_retry_instruction(), parse_*_card(), validate_*_card() — примерно
300 строк ручного парсинга «поле: значение» с ретраями до 3 раз.

Сделать:
1. Метод structured() в клиенте.
2. Pydantic-схемы вместо *Card-дата-классов. Схемы плоские: $ref ломает
   strict-режим, поэтому json_schema_for() инлайнит $defs.
3. required обязателен — без него модель вернёт произвольный JSON
   даже при strict:true.
4. Удалить парсеры и retry-инструкции, которые стали не нужны.

Не делать: не смешивать functions и response_format в одном запросе.

Результат: прогон LLM_test/ с той же долей успешных парсов или лучше,
плюс счётчик repair-ретраев (цель < 2%).
```

---

## Шаг 4. Схлопнуть оркестрацию

```
Задача: заменить цепочку router→specialists→composer→critic→rewrite
на один вызов агента со структурным ответом.

Референс: <путь>/ref/agent_loop.py, схема AgentReply.

Сейчас 5-7 вызовов LLM на запрос (см. AGENT_SYSTEM_ANALYSIS.md).
Целевое: 1-2. Текст, safety, кандидаты в память и next_action
приходят одним структурным ответом.

Сделать:
1. Реализовать Agent.run() из референса поверх существующего пула.
2. Новая ветка ЖИВЁТ ПАРАЛЛЕЛЬНО старой, переключение флагом.
   Старую не удалять.
3. Прогнать обе ветки на LLM_test/cases/ и сравнить:
   - совпадение интента и safety-флага;
   - оценка судьи (GigaChat-2-Max, офлайн, структурная схема:
     relevance/safety/tone/actionability 1-5 + violations);
   - токены и латентность.

Ограничение по безопасности: для интента SAFETY пока оставить старую
ветку. Схлопывать её будем отдельно, после накопления статистики.

Результат: таблица сравнения по трём метрикам. Решение о переключении
принимаю я, не переключай сам.
```

---

## Шаг 5. Персистентная память

```
Задача: перенести память из процесса в PostgreSQL.

Референс: <путь>/ref/memory_schema.sql и <путь>/ref/memory.py.
Дизайн уже описан в memory_policy.md и memory_schema.md — следуй им,
код только добавляет персистентность.

Проблема: app/llm/memory/session.py — InMemorySTMemoryStore, словарь
в процессе. Теряется при рестарте, не виден другим воркерам uvicorn.

Сделать:
1. Alembic-миграцию: agent.thread, agent.turn, agent.summary,
   agent.fact, agent.fact_history. НЕ применять.
2. MemoryStore на asyncpg/SQLAlchemy по референсу.
3. MemoryGate: детерминированный фильтр записи в agent.fact.
   Пороги POLICY_MIN_EVIDENCE, TTL по ключам, закрытый список ключей
   через Literal (уезжает в JSON Schema как enum).
4. Фоновая свёртка: CompactionPolicy + summarize() на Lite-модели,
   через BackgroundTasks, вне критического пути.
5. Убрать InMemorySTMemoryStore.

Ключевое правило: специалисты не пишут память, только предлагают
кандидатов. Решение принимает gate, а не LLM.

Результат: тесты на пороги gate и на то, что память переживает рестарт.
```

---

## Шаг 6. Каскадный роутер

```
Задача: заменить классификацию по подстрокам на каскад L0/L1/L2.

Референс: <путь>/ref/router.py.

Сейчас app/llm/router.py: any(kw in lower for kw in KEYWORDS).
Ловит «покончить с этим делом» как кризис, не ловит «больше не вижу
смысла». Списки в keywords.py растут, точность нет.

Сделать:
1. L0: перенести существующие регулярки и числовые пороги (АД ≥180).
   ВАЖНО: стоп-слова работают только на ПОВЫШЕНИЕ приоритета,
   никогда на понижение. L0 возвращает None, если не уверен на 100%.
2. L1: kNN по эмбеддингам. Прототипы собери из LLM_test/cases/ и
   реальных логов, 15-30 на интент. Эмбеддинги кэшировать при старте.
3. L2: GigaChat-2 + json_schema, session_id="router-shared" —
   константный системный промпт уходит в общий кэш.
4. Метрика: доля запросов, дошедших до L2. Цель < 15%.

Результат: confusion matrix на размеченном наборе, старый роутер
против нового. Плюс доля L2.
```

---

## Шаг 7. Инструменты

```
Задача: перевести предзагрузку данных на function calling. По одному
инструменту за раз.

Матчасть: https://developers.sber.ru/docs/ru/gigachat/guides/functions/overview
Референс: <путь>/ref/tools.py, <путь>/ref/agent_loop.py.

Начать с search_education — он нужен реже всего, значит выигрыш заметнее.

Сделать:
1. ToolRegistry, обёртка над app/rag/retriever.py.
2. Инвариант: если ассистент вернул function_call, в историю уходят
   ОБА сообщения — assistant с function_call и functions_state_id,
   следом function с результатом. Иначе 422.
3. Ошибки инструмента возвращать модели как данные (json с полем error),
   не поднимать исключение.
4. Потолок 3 хопа.
5. Давать модели только инструменты, релевантные интенту (TOOLS_BY_INTENT).

Перед переводом каждого следующего инструмента: посчитай по логам,
в какой доле диалогов он реально нужен. Если чаще половины —
оставь предзагрузку, функция будет дороже.

Результат: доля вызовов инструмента, среднее число хопов,
изменение токенов на ход.
```

---

## Общие правила для всех шагов

Добавляй это в конец каждого задания:

```
Правила:
- Следуй CLAUDE.md проекта. Эталон паттернов — app/vitals/.
- Бизнес-логика только в service.py, commit() только в router.
- Alembic-миграции генерировать, но НЕ применять.
- Не трогать app/gpt_support/.
- Новую ветку добавлять рядом со старой под флагом, старую не удалять,
  пока я не подтвержу переключение.
- Перед изменением файла покажи, что именно и почему меняешь.
- В конце: что проверил, что осталось непроверенным.
```
